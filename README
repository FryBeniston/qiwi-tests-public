# QIWI Payout API Tests

Тесты написаны на основе документации:  
🔗 [https://developer.qiwi.com/ru/payout/v1/#about](https://developer.qiwi.com/ru/payout/v1/#about)

что тут есть:

1. **Postman-коллекция** — 4 теста:
   - Проверка доступности сервиса
   - Запрос баланса (> 0)
   - Создание платежа (1 рубль)
   - Исполнение платежа

2. **Playwright (TypeScript)** — автоматизированные тесты с:
   - Обработкой `text/html` 
   - Поддержкой `.env`
   - Аннотациями в отчёте

##  Как использовать

### Postman
1. Импортируйте `postman/qiwi-payout-tests.postman_collection.json`
2. Установите переменные:
   - `token` — свой токен я нигде не передавал и в тестах не использовал =) 
   - `base_url` — `https://api-test.qiwi.com/partner/payout`

### Playwright
```bash
cd playwright
npm install
cp .env.example .env
# Отредактируйте .env
npx playwright test
